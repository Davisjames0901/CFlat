using Digman.IO.CFlat.Data.Enums;

namespace Digman.IO.CFlat.SyntaxAnalyzers
{
    public class ScopeValidator: IValidator
    {
        public bool Validate(Keyword keyword, Keyword target)
        {
            return default;
        }
    }
}